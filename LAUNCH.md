# 런칭 체크리스트

메타 광고를 켜기 전에 확인해야 하는 것들입니다.
**[차단]** 항목이 하나라도 남아 있으면 판매를 시작하면 안 됩니다.

---

## 1. 사업자 / 법적 요건 — 전부 사용자가 직접 처리

| | 항목 | 상태 |
|---|---|---|
| **[차단]** | 사업자등록 | ☐ |
| **[차단]** | 통신판매업 신고 (관할 구청) | ☐ |
| **[차단]** | `config.js` 의 `biz` 항목 채우기 | ☐ |
| **[차단]** | `privacy.html` / `terms.html` / `refund.html` 의 `[대괄호]` 전부 교체 | ☐ |
| 권장 | 세 문서를 법률 전문가에게 검토받기 | ☐ |

`config.js` 를 비워두면 푸터에 **"사업자 정보 미등록"** 경고가 빨갛게 표시됩니다.
이 경고가 보이는 상태로는 판매할 수 없습니다.

전자상거래법상 표시해야 하는 항목: 상호, 대표자명, 사업자등록번호,
통신판매업 신고번호, 사업장 주소, 연락처, 이메일.

---

## 2. 결제

| | 항목 | 상태 |
|---|---|---|
| **[차단]** | PG사 계약 (토스페이먼츠 / 포트원 등) | ☐ |
| **[차단]** | `server.js` 의 `VERIFY` 블록에 **서버 측 결제 조회** 구현 | ☐ |
| **[차단]** | 운영에서 `PAYMENT_VERIFY=off` 를 **쓰지 않기** | ☐ |
| | 결제 실패 / 취소 / 중복결제 흐름 테스트 | ☐ |

### 지금 상태

주문 흐름은 만들어져 있습니다.

```
/api/order (pending 생성) → [PG 결제창] → /api/order/confirm (서버 검증 후 paid)
```

`server.js` 의 `VERIFY` 블록이 비어 있어서, 기본 모드에서는 **501 을 반환하고
주문이 `pending` 으로 남습니다.** 즉 지금 배포해도 무단으로 리포트가 열리지는 않습니다.

여기에 반드시 넣어야 하는 것:

```js
const paid = await fetchPgPayment(b.paymentKey);   // PG 서버에 조회
if (!paid || paid.status !== "DONE" || paid.amount !== order.amount) {
  return json(res, 402, { error: "payment verification failed" });
}
```

**금액을 반드시 서버에 저장된 `order.amount` 와 대조하세요.**
클라이언트가 보낸 금액을 믿으면 100원 결제로 리포트를 열 수 있습니다.

---

## 3. 배포

| | 항목 | 상태 |
|---|---|---|
| **[차단]** | 도메인 구입 | ☐ |
| **[차단]** | HTTPS 인증서 (Let's Encrypt 등) | ☐ |
| **[차단]** | 서버 바인딩을 `127.0.0.1` → `0.0.0.0` 으로 변경 (리버스 프록시 뒤라면 유지) | ☐ |
| | `data/` 디렉터리 백업 설정 | ☐ |
| | 프로세스 매니저 (pm2, systemd 등)로 재시작 보장 | ☐ |

`server.js` 마지막 줄의 `server.listen(PORT, "127.0.0.1", ...)` 가 현재 로컬 전용입니다.
Nginx 등 리버스 프록시 뒤에 둘 거라면 그대로 두고, 직접 노출한다면 바꿔야 합니다.

**`data/app.db` 에는 실제 이용자의 생년월일이 쌓입니다.**
`.gitignore` 에 이미 넣어뒀지만, 서버 백업·접근권한도 반드시 관리하세요.

---

## 4. Meta 광고

| | 항목 | 상태 |
|---|---|---|
| **[차단]** | 개인정보처리방침 URL 제출 (Meta 요구) → `/privacy.html` | ☐ |
| **[차단]** | 도메인 인증 → `config.js` 의 `meta.domainVerification` | ☐ |
| | 픽셀 생성 후 `config.js` 의 `meta.pixelId` 입력 | ☐ |
| | 전환 이벤트 `Purchase` 설정 및 테스트 | ☐ |
| | 소재 카피가 개인 속성 정책을 위반하지 않는지 확인 | ☐ |

`config.js` 의 `meta.pixelId` 가 비어 있으면 픽셀을 **아예 로드하지 않습니다.**
값을 넣으면 자동으로 로드되고, `app.js` 가 다음 표준 이벤트를 전송합니다.

| 내부 이벤트 | Meta 표준 이벤트 |
|---|---|
| `diagnosis_start` | `Lead` |
| `checkout_start` | `InitiateCheckout` |
| `purchase_complete` | `Purchase` |

### 소재 카피 주의

Meta 는 이용자의 상태를 단정하는 광고를 제한합니다.

- ❌ "요즘 회사 때문에 힘드시죠?" / "당신은 번아웃입니다"
- ⭕ "퇴사를 고민할 때 가장 어려운 질문" / "회사에 남을지 고민된다면"

---

## 5. 표시광고법 (거짓·과장 광고)

| | 항목 | 상태 |
|---|---|---|
| ✅ | 가짜 실시간 접속자 수 → **제거 완료** | ☑ |
| ✅ | 가짜 후기 / 누적 이용자 수 → **없음** | ☑ |
| ✅ | 결과 미리보기에 "예시" 표기 → **완료** | ☑ |
| | 후기를 넣는다면 **실제 이용자 동의를 받은 것만** | ☐ |
| | 통계를 넣는다면 **실제 집계된 수치만** | ☐ |

숫자를 지어내면 표시광고법 위반이고, Meta 계정 정지 사유도 됩니다.
실제 데이터가 쌓이기 전에는 넣지 마세요.
`GET /api/stats` 로 실제 집계를 확인할 수 있습니다.

---

## 6. 개인정보

| | 항목 | 상태 |
|---|---|---|
| ✅ | 수집·이용 동의 UI (필수/선택 분리) → **구현 완료** | ☑ |
| ✅ | 국외 이전 동의 (Anthropic) → **구현 완료, 미동의 시 LLM 미호출** | ☑ |
| ✅ | 회사명·상사 이름 미수집 | ☑ |
| ✅ | 주민등록번호 미수집 | ☑ |
| **[차단]** | 보유기간 확정 후 `privacy.html` 과 동의 UI 에 반영 | ☐ |
| | 보유기간 경과 데이터 자동 파기 배치 | ☐ |
| | 열람·삭제 요청 처리 절차 마련 | ☐ |

**AI 해설(국외 이전)에 동의하지 않은 이용자는 LLM 을 호출하지 않고
규칙 기반 문장으로 리포트가 생성됩니다.** 서비스는 정상 제공됩니다.

---

## 7. 운영 준비

| | 항목 | 상태 |
|---|---|---|
| | `ANTHROPIC_API_KEY` 설정 (없으면 규칙 기반으로 동작) | ☐ |
| | LLM 월 예산 한도 설정 | ☐ |
| | 문의 대응 채널 (이메일 / 채널톡 등) | ☐ |
| | 환불 요청 처리 담당자 지정 | ☐ |
| ✅ | 레이트리밋 → **구현 완료** (narrative 5회/분) | ☑ |
| ✅ | 보안 헤더 → **구현 완료** | ☑ |

---

## 실행 명령

```bash
# 개발 (결제 검증 건너뜀 — 운영 금지)
PAYMENT_VERIFY=off node server.js

# 운영
ANTHROPIC_API_KEY=sk-... node server.js
```

## 회귀 테스트

배포 전 반드시 통과시키세요.

```bash
node tests/run.js      # 사주 명식 50건 + 결정성
node tests/lunar.js    # 음력 변환 80건 + 잘못된 입력 차단
```
