/* =========================================================
   사이트 설정
   ---------------------------------------------------------
   광고를 돌리기 전에 이 파일의 값을 채우세요.
   값이 비어 있으면 해당 기능은 자동으로 꺼집니다.
   ========================================================= */
window.SITE = {
  /* ---- 사업자 정보 (전자상거래법 필수 표시사항) ----
     아래를 채우면 모든 페이지 푸터에 자동으로 반영됩니다.
     비워두면 "준비중"으로 표시되며, 이 상태로는 판매할 수 없습니다. */
  biz: {
    name: "",            // 상호
    ceo: "",             // 대표자명
    regNo: "",           // 사업자등록번호
    mailOrderNo: "",     // 통신판매업 신고번호
    address: "",         // 사업장 주소
    tel: "",             // 연락처
    email: ""            // 문의 이메일
  },

  /* ---- Meta 광고 ----
     pixelId 를 넣으면 픽셀이 로드되고 표준 이벤트가 전송됩니다.
     비워두면 픽셀을 아예 불러오지 않습니다. */
  meta: {
    pixelId: "",                 // 예: "1234567890123456"
    domainVerification: ""       // Meta 도메인 인증 메타태그 content 값
  },

  /* ---- 데모 모드 ----
     true 면 "지금은 판매하지 않는 미리보기" 로 동작합니다.
       · 결제 버튼이 결제 대신 안내를 띄웁니다
       · 사업자 정보 미등록 경고 대신 데모 안내를 보여줍니다
     사업자등록·결제 연동이 끝나면 false 로 바꾸세요. */
  demo: true,

  /* ---- 가격 ---- */
  price: 9900
};
